 <nav class="nav-wrapper">
    <a class="brand-logo" href="/">SidhuClinic</a>
    <% if user_signed_in? %>
      <a href="<%= destroy_user_session_path %>" data-method="delete" data-target="mobile-demo" class="right sidenav-trigger"><i class="material-icons">power_settings_new</i></a>
      <div class="right hide-on-med-and-down">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= button_to "Sign out #{current_user.email}", destroy_user_session_path, class:"btn", method: :delete %>
          </li>
        </ul>
      </div>
    <% end %>
  </nav>